import error from "./error";
import currentUser from "./currentUser";
import dataManagement from "./dataManagement";

export { error, currentUser, dataManagement };
export default { error, currentUser, dataManagement };
